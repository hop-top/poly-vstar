# rdate_only

No RRULE: an RDATE-only recurrence set is legal and finite. DTSTART
is occurrence #1; the RDATE values (authored out of order) sort
after it. The set is `complete` — nothing could continue it.
