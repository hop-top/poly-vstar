# bysetpos

`BYSETPOS=-1` paired with `BYDAY=MO,TU,WE,TH,FR` under MONTHLY:
positional filter picks the last weekday of each month — the
canonical "last business day" pattern. In v0.2 scope per spec/03
§RRULE parsing scope.
