# until_form1_local

UNTIL must be RFC 5545 form #2 (UTC, Z-suffixed). Form #1 (local)
is rejected — V*'s strict-UTC posture for RRULE bounds (spec/03
§RRULE parsing scope) carries the VTIMEZONE subset's UTC-only
stance forward.
